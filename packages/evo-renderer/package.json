{
  "name": "@evo/renderer",
  "version": "0.1.0",
  "description": "Canvas-based generative art renderer for EVO (Evolving Value Object)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "keywords": ["solana", "evo", "generative-art", "canvas", "renderer"],
  "license": "MIT",
  "dependencies": {
    "@evo/sdk": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "rimraf": "^6.0.0"
  }
}